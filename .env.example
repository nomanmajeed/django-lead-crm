# Copy to .env at the project root and adjust as needed.
#   cp .env.example .env

DEBUG=True
SECRET_KEY=dev-only-change-me-django-lead-crm-local
READ_DOT_ENV_FILE=True

# Local PostgreSQL (docker compose up -d db) — host port 5433
DATABASE_URL=postgres://leadcrm:leadcrm@127.0.0.1:5433/leadcrm
